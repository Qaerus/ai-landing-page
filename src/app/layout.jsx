export const metadata = {
  title: "Dustin Jones Outreach",
  description: "Cold email appointment setting for local businesses",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head />
      <body>{children}</body>
    </html>
  );
}